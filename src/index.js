import React from 'react';
import ReactDOM from 'react-dom';
import Work from '../src/componnts/work';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <Work />
  </React.StrictMode>
);